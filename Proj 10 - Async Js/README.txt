如果要测试S5，请解除server.js文件中的如下注释：

// if (Math.random() > 0.75) { res.writeHead(500, {}); res.end(); return; }
^~~
解除这里注释

反之若要测试S1~S4，请将这句代码加上注释。

另外，为了防止机器人手速太快看不到大气泡被激活的过程，所有的大气泡点击事件都故意推迟了1s执行

1.1更新：
不需要手动注释代码了，现在可以自动判断什么时候要进行随机失败了